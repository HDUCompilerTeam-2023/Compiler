#ifndef __MIR_TEST_BASIC_BLOCK_DEFINE__
#define __MIR_TEST_BASIC_BLOCK_DEFINE__

#include <mir.h>

#include <mir_test/memory_stack.h>

memory_type mir_basic_block_test(p_list_head p_list, memory_stack **global_stack, memory_stack **top_stack);

#endif