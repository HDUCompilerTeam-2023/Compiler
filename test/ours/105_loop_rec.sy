int main()
{
    int sum = 0, i = 0; 
    int global = getint();
    int loop = getint();
    while(i < loop)
    {
        int tmp = 0;
        int j = 0;
        while(j < 60)
        {
            tmp = tmp + global;
            j = j + 1;
        }
        tmp = tmp / 60;
        sum = sum + tmp;
        sum = sum % 133333333;
        i = i + 1;
    }
    return sum;
}